feat(miniprogram): 打通智能匹配与我的发布后端链路 + 成果库字段降级

【智能匹配页接真实接口】（pkg-demand/pages/demands/matches.vue）
- 此前纯 mock（getKindItems），生产显示空列表；后端 GET /api/v1/match
  早已存在却从未被调用
- 重写：关键词搜索 + 快捷分类 chips → GET /api/v1/match?q=&limit=
- 展示真实 MatchResult：匹配度（score）、业务类型标签、简介、
  地区/发布时间、匹配理由标签；点击跳需求详情
- 完整状态：初始引导 / 加载 / 无结果（可去大厅）/ 结果列表

【我的发布接后端】（pages/publish/my-posts.vue + index.vue）
- 此前列表只读本地 publishData，清缓存/换设备即空（后端数据在却看不到）
- 改造：GET /api/v1/demands?mine=1 + /api/v1/products?mine=1 拉取
  后端已提交记录，与本地草稿/service/course 记录（已知边界）合并去重
- 状态映射：demand(pending/published/matched/completed/rejected)、
  product(listed/sold/off)；后端记录点击跳真实详情页
- 发布 tab 页"我的发布"计数并入后端数据

【成果库字段降级】（pkg-eco/pages/achievements/list.vue）
- 后端 Achievement 无 views/favs 字段：为 0 时不再显示"0 浏览/0 收藏"
  （此前生产环境每个卡片都挂着 0 0）

后端零改动（接口均已存在）；纯前端打通。
