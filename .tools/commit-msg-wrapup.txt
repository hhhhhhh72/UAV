feat: 收尾批次——ctx 全链路传播 + 上传配额 + 限流修正 + CI 前端构建 + 部署清单

【ctx 全链路传播】请求级取消/超时从 Handler 精确传导到 SQL 查询：
- 329 个 Repository 接口方法、全部 PG/Memory 实现、service 层方法统一
  首参 context.Context；httpapi 调用点透传 r.Context()
- 异步通知 goroutine 改用 context.Background()（r.Context() 在 handler
  返回后取消会中断通知发送）
- 修复 PG repo 内部跨方法调用未传 ctx 的编译断点（FindByID 等 6 处）
- 修复 memory demandRepo.Search 内部 List 调用

【上传配额】uploads 台账表（迁移 000067）+ 按用户每日字节配额：
- UPLOAD_DAILY_QUOTA_BYTES 默认 50MB，超限 413 且文件不落盘
- 顺带修复 Windows 下文件句柄未关闭导致超限文件残留
- 修复 Upload/UploadPrivate visibility 恒为 private 的旧 bug

【限流修正】反代后按 X-Forwarded-For 首跳 IP 分桶（此前全站共享单桶）；
API 端口收紧为仅回环 127.0.0.1:8080，杜绝伪造 XFF 绕过限流

【CI】新增 frontend job（npm ci + vite build）

【文档】docs/运维部署/上线部署清单.md（备份/打包/发布/回滚全流程）

验证：go build/vet 全零告警；全量测试通过（postgres 连真库）；
覆盖率 78.8%（service 92.2% / postgres 85.2% / memory 88.8%）
