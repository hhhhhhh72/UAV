# 缓存机制设计

<cite>
**本文档引用的文件**
- [cache.js](file://backend/cache.js)
- [storage.js](file://backend/storage.js)
- [index.js](file://backend/index.js)
- [config.js](file://backend/config.js)
- [admin.js](file://backend/routes/admin.js)
- [migrate.js](file://backend/db/migrate.js)
- [schema.sql](file://backend/db/schema.sql)
- [package.json](file://backend/package.json)
- [nginx.conf](file://frontend/h5/nginx.conf)
- [OPTIMIZATION_SUMMARY.md](file://backend/OPTIMIZATION_SUMMARY.md)
- [QUICK_REFERENCE.md](file://backend/QUICK_REFERENCE.md)
- [REFACTORING.md](file://backend/REFACTORING.md)
</cite>

## 更新摘要
**变更内容**
- 新增图像压缩缓存实现细节，包括磁盘缓存目录结构和缓存键生成策略
- 添加Nginx层缓存配置说明，实现多层缓存架构
- 增强WebP格式支持和客户端适配机制
- 扩展缓存系统性能优化方案

## 目录
1. [引言](#引言)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [图像压缩缓存系统](#图像压缩缓存系统)
7. [Nginx多层缓存架构](#nginx多层缓存架构)
8. [依赖关系分析](#依赖关系分析)
9. [性能考虑](#性能考虑)
10. [故障排除指南](#故障排除指南)
11. [结论](#结论)
12. [附录](#附录)

## 引言

本项目实现了基于内存的缓存系统，旨在优化数据访问性能，减少对底层存储层的频繁读取。缓存机制采用简单高效的内存映射表实现，支持多种数据类型的差异化过期策略，并提供了完整的缓存生命周期管理功能。

**更新** 新增图像压缩缓存系统，实现磁盘缓存和Nginx反向代理缓存的多层架构，显著提升图片资源的加载性能。

缓存系统的核心设计理念是：
- **高性能**：内存访问速度远超磁盘IO
- **灵活性**：支持不同数据类型的差异化缓存策略
- **可靠性**：自动过期清理和错误处理
- **可维护性**：清晰的键值管理和统计监控
- **可扩展性**：支持多层缓存架构和格式适配

## 项目结构

缓存机制相关的文件组织如下：

```mermaid
graph TB
subgraph "缓存层"
Cache[Cache类<br/>内存缓存]
ImageCache[图像缓存<br/>.image-cache目录]
CacheKeys[缓存键常量]
end
subgraph "存储层"
Storage[Storage接口<br/>JSON文件/PostgreSQL]
JSONFiles[JSON文件<br/>users.json, cases.json, data.json]
PGTable[(PostgreSQL表<br/>json_store)]
end
subgraph "应用层"
Routes[路由处理<br/>admin.js]
Config[配置管理<br/>config.js]
ImageAPI[图像压缩API<br/>/api/image]
end
subgraph "反向代理层"
Nginx[Nginx反向代理<br/>proxy_cache配置]
end
Cache --> Storage
ImageCache --> ImageAPI
CacheKeys --> Cache
Storage --> JSONFiles
Storage --> PGTable
Routes --> Storage
Config --> Storage
ImageAPI --> ImageCache
Nginx --> ImageAPI
```

**图表来源**
- [cache.js:1-119](file://backend/cache.js#L1-L119)
- [storage.js:1-197](file://backend/storage.js#L1-L197)
- [index.js:91-153](file://backend/index.js#L91-L153)
- [nginx.conf:1-116](file://frontend/h5/nginx.conf#L1-L116)

**章节来源**
- [cache.js:1-119](file://backend/cache.js#L1-L119)
- [storage.js:1-197](file://backend/storage.js#L1-L197)
- [index.js:91-153](file://backend/index.js#L91-L153)

## 核心组件

### 缓存类设计

缓存系统的核心是一个简洁的内存缓存类，提供以下核心功能：

#### 基础操作
- **get(key)**: 获取缓存项，自动检查过期时间
- **set(key, value, ttl)**: 设置缓存项，支持自定义过期时间
- **delete(key)**: 删除特定缓存项
- **clear()**: 清空所有缓存

#### 高级功能
- **getOrSet(key, fn, ttl)**: 命中则返回，未命中则异步加载并设置缓存
- **cleanup()**: 清理过期缓存项
- **stats()**: 获取缓存统计信息

#### 缓存键管理

系统定义了统一的缓存键常量，确保键值的一致性和可维护性：

| 键类型 | 键值模式 | 描述 |
|--------|----------|------|
| 用户数据 | `db:users` | 用户基础数据缓存 |
| 申请数据 | `db:applications` | 业务申请数据缓存 |
| 案例数据 | `db:cases` | 展示案例数据缓存 |
| 服务配置 | `db:services_config` | 服务配置数据缓存 |
| 评价数据 | `db:reviews` | 用户评价数据缓存 |
| 管理统计 | `stats:admin` | 管理后台统计缓存 |
| 用户详情 | `user:{userId}` | 特定用户的详细信息缓存 |

**章节来源**
- [cache.js:54-99](file://backend/cache.js#L54-L99)
- [cache.js:107-116](file://backend/cache.js#L107-L116)

## 架构概览

缓存系统采用分层架构设计，实现了存储层与应用层的有效解耦：

```mermaid
sequenceDiagram
participant Client as 客户端
participant Nginx as Nginx反向代理
participant Route as 路由处理器
participant ImageCache as 图像磁盘缓存
participant Sharp as 图像处理
participant Storage as 存储接口
participant Cache as 内存缓存
participant Store as 底层存储
Client->>Nginx : 发起图片请求
Nginx->>Route : 检查本地缓存
Route->>ImageCache : 检查磁盘缓存
alt 磁盘缓存命中
ImageCache-->>Route : 返回缓存图片
Route-->>Nginx : 返回图片
Nginx-->>Client : 响应图片
else 磁盘缓存未命中
Route->>Sharp : 处理图片压缩
Sharp->>ImageCache : 写入缓存
ImageCache-->>Route : 缓存成功
Route-->>Nginx : 返回处理后的图片
Nginx-->>Client : 响应图片
end
```

**图表来源**
- [index.js:98-153](file://backend/index.js#L98-L153)
- [nginx.conf:82-96](file://frontend/h5/nginx.conf#L82-L96)

## 详细组件分析

### 缓存策略设计

#### 时间过期策略

系统针对不同类型的数据制定了差异化的过期时间策略：

| 数据类型 | 过期时间 | 设计原因 | 缓存键 |
|----------|----------|----------|--------|
| 用户数据 | 60秒 | 用户信息频繁变更，需要及时更新 | `db:users` |
| 案例数据 | 5分钟 | 展示内容相对稳定，减少频繁读取 | `db:cases` |
| 申请数据 | 1分钟 | 业务数据实时性要求高 | `db:applications` |
| 服务配置 | 5分钟 | 配置变更频率较低 | `db:services_config` |
| 评价数据 | 60秒 | 评价内容需要快速响应 | `db:reviews` |

#### 过期时间计算机制

```mermaid
flowchart TD
Start([开始缓存操作]) --> CheckCache["检查缓存是否存在"]
CheckCache --> CacheExists{"缓存存在？"}
CacheExists --> |是| CheckExpiry["检查是否过期"]
CacheExists --> |否| LoadData["加载数据"]
CheckExpiry --> Expired{"已过期？"}
Expired --> |是| LoadData
Expired --> |否| ReturnCache["返回缓存数据"]
LoadData --> SetCache["设置新缓存"]
SetCache --> ReturnData["返回数据"]
ReturnCache --> End([结束])
ReturnData --> End
```

**图表来源**
- [cache.js:14-28](file://backend/cache.js#L14-L28)
- [cache.js:33-38](file://backend/cache.js#L33-L38)

**章节来源**
- [storage.js:134-181](file://backend/storage.js#L134-L181)

### 缓存读取流程

缓存读取采用"先命中后加载"的策略，确保数据的时效性和一致性：

```mermaid
sequenceDiagram
participant API as API调用方
participant Storage as Storage接口
participant Cache as 缓存系统
participant FS as 文件系统
participant PG as PostgreSQL
API->>Storage : readUsersDB()
Storage->>Cache : getOrSet("db : users", callback, 60s)
alt 缓存命中且未过期
Cache-->>Storage : 返回缓存用户数据
Storage-->>API : 返回用户数据
else 缓存未命中或已过期
Cache->>Storage : 执行callback()
alt 使用JSON文件存储
Storage->>FS : 读取users.json
FS-->>Storage : 返回用户数据
else 使用PostgreSQL存储
Storage->>PG : 查询json_store表
PG-->>Storage : 返回用户数据
end
Storage->>Cache : set("db : users", 数据, 60s)
Cache-->>Storage : 缓存设置成功
Storage-->>API : 返回用户数据
end
```

**图表来源**
- [storage.js:134-142](file://backend/storage.js#L134-L142)
- [cache.js:57-67](file://backend/cache.js#L57-L67)

**章节来源**
- [storage.js:134-142](file://backend/storage.js#L134-L142)

### 缓存写入策略

缓存写入采用"先写存储后清缓存"的策略，确保数据的一致性：

```mermaid
flowchart TD
WriteStart([开始写入操作]) --> ClearCache["清除相关缓存"]
ClearCache --> WriteStore["写入底层存储"]
WriteStore --> WriteSuccess{"写入成功？"}
WriteSuccess --> |是| Complete([完成])
WriteSuccess --> |否| HandleError["处理写入错误"]
HandleError --> Complete
```

**图表来源**
- [storage.js:139-142](file://backend/storage.js#L139-L142)
- [storage.js:149-152](file://backend/storage.js#L149-L152)

**章节来源**
- [storage.js:139-142](file://backend/storage.js#L139-L142)

### 缓存失效机制

系统实现了多层次的缓存失效机制：

#### 自动清理
- **定时清理**：每5分钟自动清理过期缓存项
- **惰性清理**：访问时检查并清理过期项

#### 手动失效
- **写入触发**：数据写入后立即清除相关缓存
- **键空间管理**：支持按键前缀批量清理

**章节来源**
- [cache.js:72-79](file://backend/cache.js#L72-L79)
- [cache.js:105](file://backend/cache.js#L105)

### 缓存键值管理

#### 键值设计原则
- **命名规范**：采用`namespace:key`格式
- **命名空间隔离**：不同数据类型使用独立命名空间
- **动态键生成**：支持用户ID等动态参数

#### 键值映射表

| 键名 | 命名空间 | 动态参数 | 用途 |
|------|----------|----------|------|
| `db:users` | db | 无 | 用户基础数据 |
| `db:applications` | db | 无 | 业务申请数据 |
| `db:cases` | db | 无 | 展示案例数据 |
| `db:services_config` | db | 无 | 服务配置数据 |
| `db:reviews` | db | 无 | 用户评价数据 |
| `stats:admin` | stats | 无 | 管理统计数据 |
| `user:{userId}` | user | userId | 用户详情数据 |

**章节来源**
- [cache.js:108-116](file://backend/cache.js#L108-L116)

## 图像压缩缓存系统

### 磁盘缓存目录结构

系统实现了专门的图像压缩缓存机制，使用 `.image-cache` 目录存储处理后的图片：

```mermaid
graph TB
subgraph "图像缓存目录结构"
ImageCacheDir[.image-cache/<br/>磁盘缓存目录]
CacheKey[缓存键生成<br/>基于源文件+参数]
CacheFile[缓存文件<br/>格式: {basename}_{width}_{quality}_{format}_{mtime}.{ext}]
end
subgraph "缓存键生成策略"
SourceFile[源文件路径]
WidthParam[宽度参数]
QualityParam[质量参数]
FormatParam[格式参数]
MTimeParam[修改时间参数]
end
ImageCacheDir --> CacheKey
CacheKey --> CacheFile
SourceFile --> CacheKey
WidthParam --> CacheKey
QualityParam --> CacheKey
FormatParam --> CacheKey
MTimeParam --> CacheKey
```

**图表来源**
- [index.js:93-96](file://backend/index.js#L93-L96)
- [index.js:116-119](file://backend/index.js#L116-L119)

### 缓存键生成策略

缓存键采用组合哈希策略，确保唯一性和有效性：

#### 缓存键组成要素
- **文件基础名**：去除扩展名的源文件名
- **宽度参数**：目标图片宽度
- **质量参数**：压缩质量百分比
- **输出格式**：目标格式（webp/jpg）
- **修改时间**：源文件最后修改时间戳

#### 缓存键生成公式
```
cacheKey = basename(sourceFile) + "_" + width + "_" + quality + "_" + format + "_" + mtime
cachePath = IMAGE_CACHE_DIR + "/" + cacheKey + "." + extension
```

#### 缓存文件命名规范
- **文件名格式**：`{basename}_{width}_{quality}_{format}_{mtime}.{ext}`
- **扩展名**：根据输出格式确定（webp/jpg）
- **目录结构**：扁平化存储，无子目录层级

**章节来源**
- [index.js:93-96](file://backend/index.js#L93-L96)
- [index.js:116-119](file://backend/index.js#L116-L119)

### WebP格式支持

系统实现了智能的图片格式适配机制：

#### 格式检测流程
```mermaid
flowchart TD
Start([开始格式检测]) --> CheckAccept["检查Accept头"]
CheckAccept --> HasWebP{"支持WebP？"}
HasWebP --> |是| UseWebP["使用WebP格式"]
HasWebP --> |否| UseJPEG["使用JPEG格式"]
UseWebP --> SetExt["设置扩展名: webp"]
UseJPEG --> SetExt
SetExt --> SetContentType["设置Content-Type"]
SetContentType --> End([完成])
```

**图表来源**
- [index.js:109-114](file://backend/index.js#L109-L114)

#### 输出格式特性
- **WebP格式**：更小的文件体积，现代浏览器支持
- **JPEG格式**：广泛兼容性，传统格式
- **质量控制**：可配置压缩质量（默认75%）

**章节来源**
- [index.js:109-114](file://backend/index.js#L109-L114)
- [index.js:133-137](file://backend/index.js#L133-L137)

### 图像处理流程

```mermaid
sequenceDiagram
participant Client as 客户端
participant API as /api/image
participant Cache as 磁盘缓存检查
participant Sharp as 图像处理
participant FS as 文件系统
Client->>API : GET /api/image?url=...&width=...&quality=...
API->>Cache : 检查缓存文件
alt 缓存命中
Cache-->>API : 返回缓存文件
API-->>Client : 响应压缩图片
else 缓存未命中
Cache->>Sharp : 处理图片
Sharp->>FS : 写入缓存文件
FS-->>Sharp : 缓存成功
Sharp-->>API : 处理完成
API-->>Client : 响应压缩图片
end
```

**图表来源**
- [index.js:98-153](file://backend/index.js#L98-L153)

**章节来源**
- [index.js:98-153](file://backend/index.js#L98-L153)

## Nginx多层缓存架构

### 反向代理缓存配置

系统实现了基于Nginx的反向代理缓存层，提供额外的性能优化：

#### Nginx缓存配置详解
```nginx
# 定义缓存区域
proxy_cache_path /var/cache/nginx/img levels=1:2 keys_zone=img_cache:10m max_size=500m inactive=30d use_temp_path=off;

# 图片压缩API缓存配置
location /low-altitude/api/image {
    proxy_pass http://172.17.0.1:8090/api/image;
    
    # 启用缓存
    proxy_cache img_cache;
    proxy_cache_key "$request_uri$http_accept";
    proxy_cache_valid 200 30d;
    
    # 处理缓存过期
    proxy_cache_use_stale error timeout updating;
    
    # 添加缓存状态头
    add_header X-Cache-Status $upstream_cache_status;
}
```

#### 缓存参数说明
- **levels=1:2**：两级目录结构，提高大文件系统的性能
- **keys_zone=img_cache:10m**：缓存区域大小10MB
- **max_size=500m**：最大缓存空间500MB
- **inactive=30d**：30天不活跃自动清理
- **proxy_cache_valid 200 30d**：HTTP 200响应缓存30天

### 多层缓存架构

系统实现了三层缓存架构，最大化缓存命中率：

```mermaid
graph TB
Client[客户端] --> NginxCache[Nginx反向代理缓存]
NginxCache --> DiskCache[磁盘缓存(.image-cache)]
DiskCache --> MemoryCache[内存缓存]
MemoryCache --> Database[数据库/文件系统]
```

**图表来源**
- [nginx.conf:1](file://frontend/h5/nginx.conf#L1)
- [nginx.conf:90-95](file://frontend/h5/nginx.conf#L90-L95)

### 缓存状态监控

Nginx提供了详细的缓存状态监控能力：

#### 缓存状态头
- **X-Cache-Status**：显示缓存命中状态
  - `MISS`：缓存未命中
  - `HIT`：缓存命中
  - `BYPASS`：绕过缓存
  - `EXPIRED`：缓存过期

#### 缓存清理机制
- **自动清理**：基于inactive参数的定期清理
- **内存管理**：基于keys_zone大小的内存控制
- **磁盘空间**：基于max_size的磁盘空间控制

**章节来源**
- [nginx.conf:1](file://frontend/h5/nginx.conf#L1)
- [nginx.conf:90-95](file://frontend/h5/nginx.conf#L90-L95)

## 依赖关系分析

缓存系统与其他组件的依赖关系如下：

```mermaid
graph TB
Cache[cache.js<br/>Cache类] --> Storage[storage.js<br/>Storage接口]
Cache --> Logger[logger.js<br/>日志记录]
ImageCache[index.js<br/>图像缓存API] --> Sharp[sharp库<br/>图像处理]
ImageCache --> FS[文件系统<br/>磁盘缓存]
NginxCache[nignx.conf<br/>反向代理缓存] --> ImageCache
Storage --> Cache
Storage --> DB[db/pg.js<br/>数据库连接]
Storage --> FS[文件系统<br/>JSON文件]
Routes[admin.js<br/>管理路由] --> Storage
Config[config.js<br/>配置管理] --> Storage
DB --> Schema[schema.sql<br/>数据库模式]
DB --> Migrate[migrate.js<br/>数据迁移]
Cache -.-> Package[package.json<br/>依赖包]
ImageCache -.-> Package
NginxCache -.-> Package
```

**图表来源**
- [cache.js:1-119](file://backend/cache.js#L1-L119)
- [storage.js:1-197](file://backend/storage.js#L1-L197)
- [index.js:91-153](file://backend/index.js#L91-L153)
- [nginx.conf:1-116](file://frontend/h5/nginx.conf#L1-L116)

**章节来源**
- [package.json:12-28](file://backend/package.json#L12-L28)

### 外部依赖

缓存系统主要依赖以下外部组件：

- **Node.js内置模块**：fs、path、Map
- **第三方库**：express、pg、winston、sharp
- **数据库**：PostgreSQL JSONB存储
- **Web服务器**：Nginx反向代理缓存

**章节来源**
- [package.json:12-28](file://backend/package.json#L12-L28)

## 性能考虑

### 缓存命中率优化

#### 访问模式优化
- **热点数据预热**：在应用启动时预加载常用数据
- **批量读取**：利用Promise.all并发读取多个缓存项
- **缓存预填充**：在数据更新后主动填充相关缓存

#### 内存管理策略
- **LRU淘汰**：当前实现为FIFO，可扩展为LRU算法
- **内存监控**：定期检查缓存大小和内存使用情况
- **自动清理**：定时清理过期数据释放内存

### 图像缓存性能优化

#### 多层缓存优化
- **Nginx缓存**：处理静态图片请求，减轻应用服务器压力
- **磁盘缓存**：持久化存储压缩后的图片，避免重复处理
- **内存缓存**：缓存热点数据，提供毫秒级响应

#### 缓存预热策略
- **启动预热**：应用启动时预加载常用图片
- **访问预热**：检测到热点图片时自动缓存
- **批量预热**：定期扫描热门图片进行缓存

### 性能调优方案

#### 缓存参数调优
- **过期时间调整**：根据数据变更频率调整过期时间
- **并发控制**：避免缓存击穿，实现分布式锁
- **缓存分片**：大数据集采用分片缓存策略
- **缓存预热**：热点数据提前加载到缓存

#### 监控指标
- **命中率统计**：计算缓存命中次数与总请求次数比
- **响应时间**：监控缓存读取和写入的平均响应时间
- **内存使用**：跟踪缓存占用的内存大小
- **磁盘使用**：监控图像缓存目录的空间使用情况
- **Nginx缓存状态**：监控反向代理缓存命中率

**章节来源**
- [cache.js:84-98](file://backend/cache.js#L84-L98)
- [nginx.conf:90-95](file://frontend/h5/nginx.conf#L90-L95)

### 内存管理

#### 当前实现特点
- **内存映射表**：使用ES6 Map作为底层存储
- **垃圾回收**：依赖JavaScript引擎的垃圾回收机制
- **内存泄漏防护**：自动过期清理防止内存泄漏

#### 扩展建议
- **内存限制**：实现最大缓存条目数限制
- **内存预警**：当内存使用超过阈值时触发清理
- **持久化备份**：实现缓存数据的持久化存储

## 故障排除指南

### 常见问题及解决方案

#### 缓存未生效
**症状**：数据频繁从存储层读取
**排查步骤**：
1. 检查缓存键是否正确
2. 验证过期时间设置
3. 确认getOrSet回调函数执行

#### 缓存数据过期
**症状**：返回旧数据
**解决方案**：
1. 检查定时清理任务是否正常运行
2. 验证过期时间计算逻辑
3. 确认手动清理触发时机

#### 内存使用过高
**症状**：内存占用持续增长
**处理措施**：
1. 检查是否有未清理的缓存键
2. 优化过期时间设置
3. 实现内存使用监控

#### 图像缓存问题
**症状**：图片压缩失败或缓存无效
**排查步骤**：
1. 检查.sharp库安装是否正确
2. 验证.image-cache目录权限
3. 确认磁盘空间充足
4. 检查源图片文件是否存在

#### Nginx缓存问题
**症状**：反向代理缓存未生效
**解决方案**：
1. 检查proxy_cache_path配置
2. 验证缓存目录权限
3. 确认proxy_cache指令正确配置
4. 检查缓存键生成逻辑

### 错误处理机制

缓存系统实现了完善的错误处理：

```mermaid
flowchart TD
Operation[缓存操作] --> TryOperation["尝试执行操作"]
TryOperation --> Success{"操作成功？"}
Success --> |是| ReturnResult["返回结果"]
Success --> |否| LogError["记录错误日志"]
LogError --> HandleError["处理错误"]
HandleError --> ReturnFallback["返回降级结果"]
ReturnResult --> End([结束])
ReturnFallback --> End
```

**图表来源**
- [cache.js:14-28](file://backend/cache.js#L14-L28)
- [index.js:146-152](file://backend/index.js#L146-L152)

**章节来源**
- [cache.js:14-28](file://backend/cache.js#L14-L28)
- [index.js:146-152](file://backend/index.js#L146-L152)

## 结论

本缓存机制设计实现了以下目标：

### 设计优势
- **简单高效**：基于内存的简单实现，性能优异
- **灵活配置**：支持不同数据类型的差异化缓存策略
- **可靠稳定**：自动过期清理和错误处理机制
- **易于维护**：清晰的键值管理和统计监控
- **多层优化**：结合内存、磁盘和反向代理缓存的多层次架构
- **格式适配**：智能的WebP/JPEG格式选择机制

### 技术特色
- **异步加载**：getOrSet支持异步数据加载
- **自动清理**：定时和惰性双重清理机制
- **统计监控**：提供详细的缓存使用统计
- **存储无关**：与底层存储实现完全解耦
- **性能优化**：多层缓存架构显著提升性能
- **格式兼容**：支持现代WebP格式和传统JPEG格式

### 改进建议
- **分布式支持**：考虑Redis等分布式缓存
- **监控增强**：集成APM工具进行性能监控
- **容量管理**：实现智能的内存容量控制
- **一致性保证**：加强缓存与存储的一致性
- **缓存预热**：实现智能的热点数据预热机制
- **缓存分片**：大数据集采用分片缓存策略

该缓存系统为平台提供了可靠的性能优化基础，能够有效提升数据访问效率，降低存储层压力，为后续的功能扩展和性能优化奠定了良好基础。

## 附录

### 缓存配置参考

#### 默认配置
- **默认过期时间**：60秒
- **清理间隔**：5分钟
- **存储类型**：JSON文件或PostgreSQL
- **图像缓存目录**：`.image-cache`
- **Nginx缓存区域**：`img_cache`

#### 环境变量配置
- `USE_POSTGRES`：启用PostgreSQL存储
- `JWT_SECRET`：JWT密钥
- `ACCESS_TOKEN_TTL`：访问令牌过期时间
- `REFRESH_TOKEN_TTL`：刷新令牌过期时间

#### Nginx缓存配置
- **缓存路径**：`/var/cache/nginx/img`
- **缓存区域**：`img_cache:10m`
- **最大缓存**：`500m`
- **缓存有效期**：`30d`
- **缓存键**：`$request_uri$http_accept`

**章节来源**
- [config.js:8-76](file://backend/config.js#L8-L76)
- [nginx.conf:1](file://frontend/h5/nginx.conf#L1)
- [nginx.conf:90-95](file://frontend/h5/nginx.conf#L90-L95)

### 性能基准测试

#### 缓存性能对比
- **内存缓存**：毫秒级响应时间
- **磁盘缓存**：亚秒级响应时间
- **Nginx缓存**：毫秒级响应时间
- **数据库缓存**：几十毫秒响应时间

#### 缓存命中率
- **内存缓存命中率**：90%+
- **磁盘缓存命中率**：95%+
- **Nginx缓存命中率**：98%+
- **整体缓存命中率**：99%+

**章节来源**
- [OPTIMIZATION_SUMMARY.md:443-456](file://backend/OPTIMIZATION_SUMMARY.md#L443-L456)
- [QUICK_REFERENCE.md:87-94](file://backend/QUICK_REFERENCE.md#L87-L94)